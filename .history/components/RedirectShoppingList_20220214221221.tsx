import React from "react";
import { StyleSheet, Text } from "react-native";

const RedirectShoppingList = () => {
  return <Text>Go To Shopping List</Text>;
};

const styles = StyleSheet.create({});

export default RedirectShoppingList;
